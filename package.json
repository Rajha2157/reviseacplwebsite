{
  "name": "acplusa-replica",
  "private": true,
  "version": "1.0.0",
  "description": "Production static replica for ACPL USA website",
  "scripts": {
    "serve": "serve frontend -l 5500",
    "deploy": "powershell -NoProfile -ExecutionPolicy Bypass -File ./publish_to_github.ps1"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
